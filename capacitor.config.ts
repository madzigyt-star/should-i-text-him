import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maddie.shoulditexthim',
  appName: 'Should I Text Him',
  webDir: 'out',
};

export default config;
